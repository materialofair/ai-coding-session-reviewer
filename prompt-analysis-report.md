# AI 对话 Prompt 技巧复盘报告

## 📊 会话概览

- **会话时间**: 2026-02-21 07:10-07:12 (约2分钟)
- **AI 模型**: GPT-5 (Codex CLI)
- **任务类型**: 后端链路分析 - 百变声音音色稳定性问题诊断
- **工具调用**: 15+ 次命令执行 (主要是 `sed`, `rg`, `cat`)

---

## ✅ 你做得好的地方

### 1. **任务边界清晰** ⭐⭐⭐⭐⭐

```
你负责后端链路分析：定位 /debug/versatile/prompt-recommend 及百变声音注册相关服务
```

**优点**:
- 明确角色定位 ("你负责后端链路分析")
- 指定具体 API 端点 (`/debug/versatile/prompt-recommend`)
- 限定分析范围 (后端层，不改代码)

**效果**: AI 没有偏离到前端或其他无关模块，精准聚焦后端服务。

---

### 2. **结构化输出要求** ⭐⭐⭐⭐⭐

```
回答：
1) 当前推荐策略为何可能导致音色变化
2) 是否有稳定性护栏（保守模板、敏感词过滤、seed控制）
3) 给出精确文件路径+行号+可执行改进建议（后端层）
```

**优点**:
- 使用编号列表强制 AI 按结构回答
- 每个问题都有明确的关注点 (原因、护栏、建议)
- 要求"精确文件路径+行号" - 这是高质量 prompt 的标志

**效果**: AI 的回复完全按照你的结构组织，包含了:
- `apps/server/app/api/vocal.py:338-400` (精确行号)
- `apps/server/app/agents/clone.py:487-538`
- 三条可执行的改进建议

---

### 3. **技术细节约束** ⭐⭐⭐⭐

```
重点看 Gemini prompt 生成模板和下游训练参数
（尤其 random_seed、style/emotion 词引导、长度约束）
```

**优点**:
- 预先指定关键变量 (`random_seed`, `style/emotion`)
- 引导 AI 关注特定技术点，避免泛泛而谈

**效果**: AI 的分析直接命中这些参数:
- `temperature=0.4`
- `random_seed=0.0`
- 280 字长度限制
- 情绪词分布

---

### 4. **明确禁止行为** ⭐⭐⭐⭐⭐

```
仅返回结论，不改代码
```

**优点**:
- 防止 AI 自作主张修改代码
- 节省 token (不生成大段代码)
- 保持分析的纯粹性

**效果**: AI 只输出了分析结论和建议，没有生成任何代码补丁。

---

## 🔧 可以优化的地方

### 1. **缺少上下文背景** ⚠️

**当前问题**:
你的 prompt 直接进入技术细节，但没有说明:
- 为什么要分析这个问题？(用户反馈音色不稳定？)
- 期望的音色稳定性标准是什么？
- 之前尝试过哪些方案？

**优化建议**:
```markdown
## 背景
用户反馈百变声音在相同输入下生成的音色不一致，怀疑是 Gemini prompt 生成的随机性导致。
已排查前端参数传递，现需定位后端 prompt 生成和训练参数配置。

## 任务
你负责后端链路分析：定位 /debug/versatile/prompt-recommend...
```

**为什么重要**:
- AI 能更好地理解问题的优先级
- 可以主动提出相关的诊断方向
- 避免分析偏离实际业务需求

---

### 2. **缺少约束条件** ⚠️

**当前问题**:
你要求"可执行改进建议"，但没有说明:
- 改进的优先级是什么？(性能 vs 稳定性 vs 可维护性)
- 是否有技术栈限制？(不能引入新依赖？)
- 改动的风险容忍度？(只能改配置 vs 可以重构逻辑)

**优化建议**:
```markdown
## 约束条件
- 优先级: 稳定性 > 性能 > 可维护性
- 不引入新的第三方依赖
- 改动需向后兼容，不影响现有 API 调用方
- 建议分为"快速修复"和"长期优化"两类
```

**为什么重要**:
- AI 的建议会更符合你的实际工程约束
- 避免提出"理论上正确但实际无法落地"的方案

---

### 3. **缺少验证标准** ⚠️

**当前问题**:
你要求"改进建议"，但没有说明如何验证改进是否有效:
- 如何测试音色稳定性？
- 需要什么样的 metrics？
- 如何对比改进前后的效果？

**优化建议**:
```markdown
## 验证标准
改进方案需包含:
1. 如何复现问题 (测试用例)
2. 如何量化音色稳定性 (metrics 定义)
3. 如何验证改进效果 (A/B 测试方案)
```

**为什么重要**:
- 确保建议是可验证的，而不是"听起来有道理"
- 方便后续实施和效果评估

---

### 4. **可以利用 AI 的推理能力** 💡

**当前问题**:
你的 prompt 是"命令式"的 (告诉 AI 做什么)，但没有利用 AI 的推理能力。

**优化建议**:
在结构化问题之前，加入开放式推理:

```markdown
## 第一步：自由推理
在回答具体问题前，请先:
1. 列出所有可能导致音色不稳定的因素 (不限于 prompt 生成)
2. 对每个因素进行可能性评估 (高/中/低)
3. 说明你的推理过程

## 第二步：结构化分析
基于上述推理，回答以下问题:
1) 当前推荐策略为何可能导致音色变化
...
```

**为什么重要**:
- AI 可能发现你没想到的问题点
- 推理过程可以帮助你理解 AI 的思考逻辑
- 避免"确认偏误" (只验证你预设的假设)

---

### 5. **可以要求对比分析** 💡

**当前问题**:
你只要求分析当前实现，但没有要求对比其他可能的方案。

**优化建议**:
```markdown
## 对比分析
对于每个改进建议，请提供:
1. 当前实现的问题
2. 至少 2 种可选方案
3. 每种方案的优缺点对比 (表格形式)
4. 推荐方案及理由
```

**为什么重要**:
- 帮助你做技术决策
- 避免"唯一解"思维
- 了解不同方案的 trade-off

---

## 📈 Prompt 优化模板

基于你的案例，这里是一个通用的"代码分析"prompt 模板:

```markdown
# 任务: [简短标题]

## 背景
[为什么要做这个分析？业务痛点是什么？]

## 目标
[期望达成的具体目标，可量化最好]

## 范围
- 包含: [明确要分析的模块/文件/功能]
- 排除: [明确不需要分析的部分]

## 约束条件
- 技术栈: [不能改动的技术选型]
- 兼容性: [向后兼容要求]
- 性能: [响应时间/吞吐量要求]
- 优先级: [稳定性/性能/可维护性的排序]

## 分析要求
请按以下结构回答:

### 1. 问题诊断
- 根因分析 (附文件路径+行号)
- 影响范围评估
- 风险等级 (高/中/低)

### 2. 现状评估
- 当前实现的优点
- 当前实现的问题
- 是否有防护措施

### 3. 改进方案
对于每个方案，提供:
- 方案描述
- 实施步骤 (精确到文件+行号)
- 预期效果
- 潜在风险
- 实施成本 (工时估算)

### 4. 验证方案
- 如何复现问题
- 如何验证改进效果
- 需要的测试用例

## 输出格式
- 使用 Markdown
- 代码片段用 ```语言 包裹
- 文件路径格式: `path/to/file.py:行号`
- 关键结论用 **加粗**

## 禁止行为
- ❌ 不要修改代码
- ❌ 不要引入新的第三方依赖
- ❌ 不要提供"理论上可行但工程上不现实"的方案
```

---

## 🎯 针对你的案例的优化版 Prompt

```markdown
# 任务: 百变声音音色稳定性问题诊断

## 背景
用户反馈使用百变声音功能时，相同的输入音频在不同时间生成的音色存在明显差异。
初步怀疑是后端 Gemini prompt 生成的随机性导致下游训练参数不稳定。
前端参数传递已排查无问题，现需定位后端服务层的根因。

## 目标
1. 定位导致音色不稳定的技术根因
2. 评估当前系统的稳定性保障措施
3. 提供可落地的改进方案 (优先快速修复)

## 范围
- **包含**:
  - `/debug/versatile/prompt-recommend` API
  - Gemini prompt 生成逻辑
  - 百变声音注册服务
  - 训练参数配置 (random_seed, style/emotion 引导, 长度约束)
- **排除**:
  - 前端参数传递
  - 音频预处理逻辑
  - 模型训练算法本身

## 约束条件
- 不引入新的第三方依赖
- 改动需向后兼容现有 API
- 优先级: 稳定性 > 可维护性 > 性能
- 改进方案分为"快速修复"(1-2天)和"长期优化"(1-2周)

## 分析要求

### 第一步: 自由推理
在回答具体问题前，请先列出所有可能导致音色不稳定的因素 (不限于 prompt 生成)，
并对每个因素进行可能性评估 (高/中/低)。

### 第二步: 结构化分析
基于上述推理，回答以下问题:

1. **根因分析**
   - 当前 prompt 生成策略为何可能导致音色变化？
   - 附精确文件路径+行号+关键代码片段

2. **稳定性护栏评估**
   - 是否有保守模板/敏感词过滤/seed 控制？
   - 当前护栏的覆盖率和有效性如何？

3. **改进方案**
   对于每个方案，提供:
   - 方案描述
   - 实施步骤 (精确到文件+行号)
   - 预期效果 (如何量化？)
   - 潜在风险
   - 实施成本 (快速修复 vs 长期优化)

4. **验证方案**
   - 如何复现音色不稳定问题？
   - 如何量化音色稳定性？(metrics 定义)
   - 如何验证改进效果？(A/B 测试方案)

## 输出格式
- 使用 Markdown
- 文件路径格式: `path/to/file.py:行号`
- 关键结论用 **加粗**
- 改进建议用表格对比不同方案

## 禁止行为
- ❌ 不要修改代码 (仅返回分析结论)
- ❌ 不要引入新的第三方依赖
- ❌ 不要提供无法验证的"理论方案"
```

---

## 📚 通用 Prompt 技巧总结

### 1. **结构化 > 自由发挥**
- ✅ 使用编号列表、表格、模板
- ❌ 避免"帮我分析一下"这种开放式问题

### 2. **约束 > 自由度**
- ✅ 明确说明"不要做什么"
- ✅ 限定输出格式、长度、技术栈
- ❌ 避免让 AI 自由发挥 (容易偏离目标)

### 3. **上下文 > 直接提问**
- ✅ 提供背景、目标、约束条件
- ❌ 避免"裸问题" (AI 不知道你的真实需求)

### 4. **可验证 > 听起来有道理**
- ✅ 要求提供测试用例、metrics、验证方案
- ❌ 避免接受"理论上可行"的方案

### 5. **对比 > 单一方案**
- ✅ 要求提供多个方案的优缺点对比
- ❌ 避免"唯一解"思维

### 6. **精确 > 模糊**
- ✅ 要求文件路径+行号+代码片段
- ❌ 避免"大概在某个文件里"

### 7. **分步 > 一次性**
- ✅ 先推理，再分析，最后给建议
- ❌ 避免一次性要求所有内容 (容易遗漏)

---

## 🎓 学习资源推荐

1. **Prompt Engineering Guide** (OpenAI 官方)
   - https://platform.openai.com/docs/guides/prompt-engineering

2. **Anthropic Prompt Library** (Claude 官方示例)
   - https://docs.anthropic.com/claude/prompt-library

3. **Learn Prompting** (系统化教程)
   - https://learnprompting.org/

4. **Prompt Patterns** (学术论文)
   - "A Prompt Pattern Catalog to Enhance Prompt Engineering with ChatGPT"

---

## 💡 下次对话时可以尝试

1. **加入"思考过程"要求**
   ```
   在给出结论前，请先展示你的推理过程:
   - 你查看了哪些文件？
   - 你排除了哪些可能性？
   - 你的假设是什么？
   ```

2. **要求"反向验证"**
   ```
   对于你的每个结论，请提供:
   - 如何证明这个结论是对的？
   - 如何证明这个结论是错的？
   - 需要什么额外信息来确认？
   ```

3. **使用"角色扮演"**
   ```
   你是一个有 10 年后端经验的架构师，
   正在 code review 一个音频处理系统。
   请用架构师的视角分析...
   ```

4. **要求"风险评估"**
   ```
   对于每个改进方案，请评估:
   - 实施风险 (高/中/低)
   - 回滚难度 (容易/中等/困难)
   - 对现有系统的影响范围
   ```

---

## 📊 你的 Prompt 评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 任务边界清晰度 | ⭐⭐⭐⭐⭐ | 明确指定后端范围，不改代码 |
| 结构化程度 | ⭐⭐⭐⭐⭐ | 使用编号列表，要求精确行号 |
| 技术细节约束 | ⭐⭐⭐⭐ | 指定关键参数，但可以更详细 |
| 上下文提供 | ⭐⭐ | 缺少背景和目标说明 |
| 约束条件 | ⭐⭐ | 缺少技术栈和兼容性约束 |
| 可验证性 | ⭐⭐ | 缺少验证标准和测试方案 |
| 输出格式要求 | ⭐⭐⭐ | 有基本要求，但可以更详细 |
| **总体评分** | **⭐⭐⭐⭐ (4/5)** | 已经很好，还有优化空间 |

---

## 🚀 行动建议

1. **立即可做** (5 分钟)
   - 在每次提问前，加上"背景"和"目标"两段
   - 明确说明"不要做什么"

2. **短期优化** (1 周内)
   - 建立自己的 prompt 模板库 (分析类、实现类、调试类)
   - 每次对话后，记录"哪些 prompt 有效，哪些无效"

3. **长期提升** (1 个月内)
   - 学习 Prompt Engineering 系统课程
   - 研究你所在领域的最佳 prompt 实践
   - 建立团队的 prompt 规范

---

**生成时间**: 2026-03-02
**分析对象**: Codex CLI 会话 `rollout-2026-02-21T15-10-42`
**报告版本**: v1.0
